{
  "name": "dns-server",
  "version": "1.0.0",
  "description": "DNS server that proxies Google DNS with domain overrides",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "tsc --watch"
  },
  "keywords": ["dns", "proxy", "typescript"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dns-packet": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/dns-packet": "^5.2.0"
  }
}
